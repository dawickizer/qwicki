name: Colyseus Game CI

on:
  push:
    paths:
      - 'colyseus-game/**'
  pull_request:
    paths:
      - 'colyseus-game/**'

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
    # Additional steps for Colyseus Game can go here
