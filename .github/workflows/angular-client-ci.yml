name: Angular Client CI

on:
  push:
    paths:
      - 'angular-client/**'
  pull_request:
    paths:
      - 'angular-client/**'

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
    # Additional steps for Angular client can go here
